void setup(){
  int brickWidth = 20;
  int brickHeight = 10;
  int bricksInBase = 15;
  int x = 0;
  int y = (brickHeight * bricksInBase) + ((brickHeight * bricksInBase)/5);
  
  size((brickWidth * bricksInBase),(brickHeight * bricksInBase) + ((brickHeight * bricksInBase)/5));
  
}
